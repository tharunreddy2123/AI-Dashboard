# OpenShift Configuration
OPENSHIFT_API_URL=https://api.rm3.7wse.p1.openshiftapps.com:6443
OPENSHIFT_TOKEN=sha256~your_token_here

# IBM watsonx.ai Configuration
WATSONX_API_KEY=your_watsonx_api_key_here
WATSONX_BASE_URL=https://eu-gb.ml.cloud.ibm.com
WATSONX_PROJECT_ID=your_watsonx_project_id_here
WATSONX_MODEL=meta-llama/llama-3-3-70b-instruct

# IBM Consulting Advantage (ICA) Configuration
# Get from: ICA portal → Settings → API Keys (must start with sk-)
ICA_API_KEY=your_ica_api_key_here
# Model to use — see ICA portal → Models for available models
ICA_MODEL=anthropic/claude-3-5-sonnet

# ChromaDB Configuration
CHROMA_PERSIST_DIR=./chroma_db

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# CORS Origins (comma-separated)
# Development: http://localhost:5173,http://localhost:3000
# Production:  https://your-frontend-domain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Environment (development / production)
ENVIRONMENT=development

# Connection retry settings
MAX_RETRIES=3
RETRY_DELAY=2
REQUEST_TIMEOUT=30
